import { ObserverVisibilty } from './observer-visibilty';

describe('ObserverVisibilty', () => {
  it('should create an instance', () => {
    const directive = new ObserverVisibilty();
    expect(directive).toBeTruthy();
  });
});
